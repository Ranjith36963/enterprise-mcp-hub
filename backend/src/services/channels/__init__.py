"""Per-user notification channel wrappers (Batch 2 Phase 6).

Imports stay lazy — ``crypto`` and ``dispatcher`` can be imported
independently; tests exercise one without loading Apprise unless needed.
"""
